# java-docker-template

Minimal Java application packaged with Docker.

## Build image
docker build -t java-docker-template:latest .

## Run
docker run --rm --name java-docker-demo java-docker-template:latest

# You will see: "Hello, Everything is cool & build is successful!"
# java-docker-template
